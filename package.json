{
  "name": "lambdastorm",
  "version": "0.0.1",
  "description": "Storm for AWS Lambda",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.2.12",
    "word-count": "^0.2.1",
    "yargs": "^3.29.0",
    "sentiment": "^1.0.2"
  }
}
